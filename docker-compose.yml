version: '3.8'

services:
  app:
    build: ./app          
    container_name: node_app
    restart: always       
    depends_on:
      - db                
    environment:
      - DB_HOST=db        
      - DB_PORT=5432
      - DB_USER=jadson.souza
      - DB_PASS=12345678

  
  db:
    image: postgres:15-alpine
    container_name: postgres_db
    restart: always
    environment:
      - POSTGRES_USER=jadson.souza
      - POSTGRES_PASSWORD=12345678
      - POSTGRES_DB=meubanco
    volumes:
      - db_data:/var/lib/postgresql/data 

  
  proxy:
    image: nginx:alpine
    container_name: nginx_proxy
    restart: always
    ports:
      - "80:80"           
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf 
    depends_on:
      - app               


volumes:
  db_data:
  